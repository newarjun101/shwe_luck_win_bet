import 'package:flutter/material.dart';

class BuildBalancePageBody extends StatelessWidget {
  const BuildBalancePageBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Hello World")
      ],
    );
  }
}
